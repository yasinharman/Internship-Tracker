# brotli + zstandard let Scrapy advertise the same Accept-Encoding a real
# Chrome does (gzip, deflate, br, zstd). Without them our encoding header is
# a giveaway that we are not a browser.
#
# scrapy-impersonate + curl_cffi replay a real browser's TLS ClientHello,
# which gets Indeed past the IP/fingerprint scoring Python's own stack fails
# outright. It is not enough on its own, though: curl_cffi has no JS engine,
# so it cannot run Cloudflare's managed challenge the way an actual browser
# does (measured 05.08.2026 - see playwright_middleware.py). playwright is
# what drives that actual browser, opt-in for indeed_cards only via
# USE_PLAYWRIGHT = True. Run `playwright install chromium` once after
# installing this file - the package alone does not ship the browser binary.
#
# openai + pydantic drive the field classifier (MultiwebsiteScraper/
# classifier.py). anthropic is deliberately absent: the file keeps a seam for
# it so models can be compared, but production settled on gpt-5.4-mini, and
# the import only happens if LLM_PROVIDER=anthropic.
altair==6.0.0
annotated-types==0.8.0
anyio==4.14.2
colorama==0.4.6
curl_cffi==0.15.0
distro==1.9.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
jiter==0.16.0
markdown-it-py==4.2.0
openai==2.49.0
pydantic==2.13.4
Pygments==2.20.0
rich==15.0.0
scrapy-impersonate==1.7.0
sniffio==1.3.1
tqdm==4.70.0
typing-inspection==0.4.2
APScheduler==3.11.2
attrs==25.4.0
Automat==25.4.16
blinker==1.9.0
Brotli==1.1.0
cachetools==6.2.4
certifi==2025.10.5
cffi==2.0.0
charset-normalizer==3.4.4
click==8.3.1
constantly==23.10.4
cryptography==46.0.3
cssselect==1.3.0
defusedxml==0.7.1
filelock==3.20.0
gitdb==4.0.12
GitPython==3.1.45
greenlet==3.2.4
hyperlink==21.0.0
idna==3.11
incremental==24.7.2
itemadapter==0.12.2
itemloaders==1.3.2
Jinja2==3.1.6
jmespath==1.0.1
jsonschema-specifications==2025.9.1
jsonschema==4.25.1
lxml==6.0.2
MarkupSafe==3.0.3
narwhals==2.14.0
numpy==2.4.0
packaging==25.0
pandas==2.3.3
parsel==1.10.0
pillow==12.0.0
playwright==1.62.0
Protego==0.5.0
protobuf==6.33.2
psycopg2==2.9.11
pyarrow==22.0.0
PyYAML==6.0.3
pyasn1==0.6.1
pyasn1_modules==0.4.2
pycparser==2.23
pydeck==0.9.1
PyDispatcher==2.0.7
pyee==13.0.1
pyOpenSSL==25.3.0
python-dateutil==2.9.0.post0
python-dotenv==1.2.1
pytz==2025.2
queuelib==1.8.0
referencing==0.37.0
requests-file==3.0.1
requests==2.32.5
rpds-py==0.30.0
Scrapy==2.13.3
service-identity==24.2.0
setuptools==80.9.0
six==1.17.0
smmap==5.0.2
SQLAlchemy==2.0.45
streamlit==1.52.2
tenacity==9.1.2
tldextract==5.3.0
toml==0.10.2
tornado==6.5.4
Twisted==25.5.0
typing_extensions==4.15.0
tzdata==2025.3
tzlocal==5.3.1
urllib3==2.5.0
w3lib==2.3.1
watchdog==6.0.0
zope.interface==8.0.1
zstandard==0.23.0
